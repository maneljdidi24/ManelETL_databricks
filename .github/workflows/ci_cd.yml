name: CI/CD PySpark ETL

on:
  push:
    branches: [ main, dev ]
  pull_request:

jobs:
  # ---------------- CI ----------------
  ci:
    name: CI - Build & Test
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run PySpark job
        run: python app.py

      - name: Run tests
        run: pytest || echo "No tests found"

  # ---------------- CD ----------------
  # ---------------- CD ----------------
  cd:
    name: CD - Docker & Databricks
    runs-on: ubuntu-latest
    needs: ci

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build and Push Docker image
        run: |
          docker build -t ${{ secrets.DOCKER_USERNAME }}/manel-etl-pyspark:latest .
          docker push ${{ secrets.DOCKER_USERNAME }}/manel-etl-pyspark:latest

      - name: Install Databricks CLI
        run: pip install databricks-cli

      - name: Deploy to Databricks Repo
        env:
          DATABRICKS_HOST: https://dbc-1397ac4c-6488.cloud.databricks.com
          DATABRICKS_TOKEN: ${{ secrets.TOKEN_DATABRICKS }}
        run: |
          # Crée le repo GitHub dans Databricks (si pas déjà créé)
          databricks repos create --url https://github.com/maneljdidi24/ManelETL_databricks.git --provider github
          # Import du script app.py directement dans le repo lié
          databricks workspace import -o app.py /Repos/maneljdidi24/ManelETL_databricks/app.py
